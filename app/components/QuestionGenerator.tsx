// QuestionGenerator removed — previously used for admin/test generation of questions.
// Keep a harmless stub so accidental imports won't break the app during cleanup.

'use client';

import React from 'react';

const QuestionGenerator: React.FC = () => {
  return null;
};

export default QuestionGenerator;
